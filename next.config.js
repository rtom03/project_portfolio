// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com'], // Add the domain hosting your images
    },
}

module.exports = nextConfig;
